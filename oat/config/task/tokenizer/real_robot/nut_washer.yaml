# Independent Stage 1 tokenizer for the 62 nut/washer demonstrations.
name: real_robot_nut_washer
task_name: nut_washer
fps: 30

shape_meta:
  action:
    shape: [7]

dataset:
  _target_: oat.dataset.real_robot_dataset.RealRobotZarrDataset
  zarr_path: /workspace/ysk/zarr/nut_washer_N62.zarr
  obs_keys: []
  action_key: action
  n_obs_steps: 0
  n_action_steps: ${horizon}
  seed: ${seed}
  val_ratio: 0.1
  max_train_episodes: null
